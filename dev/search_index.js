var documenterSearchIndex = {"docs":
[{"category":"section","location":"reference/#SimpleExpressions.jl","page":"Reference/API","text":"","title":"SimpleExpressions.jl"},{"category":"module","location":"reference/#SimpleExpressions.SimpleExpressions","page":"Reference/API","text":"SimpleExpressions\n\nA very lightweight means to create callable functions using expressions. This uses  CallableExpressions as a backend. See also  DynamicExpressions for a performant package with similar abilities.\n\nThe @symbolic macro, the lone export, can create a symbolic variable and optional symbolic parameter. When expressions are created with these variables, evaluation is deferred until the expression is called like a function. The expressions subtype Function so are intended to be useful with Julia's higher-order functions.\n\nThe expressions can be evaluated as a univariate function, u(x), a univariate function with parameter, u(x, p), or as a bivariate function, u(x,y) (with y being a parameter). These are all typical calling patterns when a function is passed to a numeric routine.  For expressions without a symbolic value (as can happen through substitution) u() will evaluate the value.\n\nTo substitute in for either the variable or the parameter, leaving a symbolic expression, we have the calling patterns u(:,p), u(x,:) to substitute in for the parameter and variable respectively. The colon can also be nothing or missing.\n\nWhen using positional arguments in a  call, as above, all symbolic variables are treated identically, as are all symbolic parameters.\n\nThere are also methods for replace that allow more complicated substitutions. For replace, symbolic objects are returned. For replace, variables are distinct and identified by their symbol. Pairs may be specified to the call notation as a convenience for replace.\n\nThere are no performance claims, this package is all about convenience.  Similar convenience is available in some form with SymPy, SymEngine, Symbolics, etc. As well, placeholder syntax is available in Underscores.jl, Chain.jl, DataPipes.jl etc., This package only has value in that it is very lightweight and, hopefully, intuitively simple.\n\nPerformance is good though, as CallableExpressions is performant. A benchmark case of finding a zero of a function runs without allocations in 1.099 μs with 0 allocations, with a symbolic expression in  1.231 μs with 0 allocations, SymEngine is two orders of magnitude slower (302.329 μs with 1731 allocations), and SymPy is about four orders slower (and with 80k allocations).\n\nExtensions are provided for SpecialFunctions, AbstractTrees, Latexify, and RecipesBase.\n\nExample\n\nusing SimpleExpressions\n@symbolic x       # (x,)\nu = sin(x) - (x - x^3/6)\nu(0.5)  # 0.000258...\nu = u - x^5/120\nu(0.5) # -1.544...e-6\n\nmap(x^2, (1, 2))  # (1, 4)\n\nusing Plots\n@symbolic x p     # (x, p)\nu = x^5 - x - p   # (x ^ 5) + (-1 * x) + (-1 * p)\nplot(u(:, 1), 0, 1.5)\nplot!(u(:, 2))    # or plot(u.(:, 1:2), 0, 1.5)\neq = cos(x) ~ 2x\nplot(eq, 0, pi/2) # like plot([eq...], 0, pi/2)\n\n\n\n\n\n","title":"SimpleExpressions.SimpleExpressions"},{"category":"type","location":"reference/#SimpleExpressions.SymbolicEquation","page":"Reference/API","text":"a ~ b\n\nCreate a SymbolicEquation.\n\nThe equation has a left and right-hand side, which can be found by tuple destructing; calling first and last; by index; or field access using .lhs and .rhs.\n\nSymbolic equations can be evaluated, in which case the value of a-b is returned.\n\nWhen a symbolic equation is passed as an argument to a symbolic expression, the pair a => b is passed to replace.\n\nThe diff function differentiates both sides.\n\nThe solve function tries to move x terms to the left-hand side; and non-x terms to the right-hand side.\n\n\n\n\n\n","title":"SimpleExpressions.SymbolicEquation"},{"category":"method","location":"reference/#Base.Unicode.isnumeric-Tuple{SimpleExpressions.AbstractSymbolic}","page":"Reference/API","text":"isnumeric(x::AbstractSymbolic)\n\nCheck if symbolic value contains a variable or a parameter. Puns on isnumeric(::Char). Use\n\n\n\n\n\n","title":"Base.Unicode.isnumeric"},{"category":"method","location":"reference/#Base.match-Tuple{Expr, SimpleExpressions.AbstractSymbolic}","page":"Reference/API","text":"match(pattern::Expr, subject::AbstractSymbolic)::MatchDict\n\nFor a pattern specified through an expression, return a dictionary of matches or a dictionary signaling failure\n\nUses vendored rule2.jl from SymbolicIntegration as this is more performant than AssociativeCommutativePatternMatching.\n\nExamples\n\njulia> @symbolic x p\n(x, p)\n\njulia> match(:(~x * cos(~y)), p*cos(x))\nBase.ImmutableDict{Symbol, SimpleExpressions.AbstractSymbolic} with 2 entries:\n  :y => x\n  :x => p\n\n\n\n\n\n","title":"Base.match"},{"category":"method","location":"reference/#Base.replace-Tuple{SimpleExpressions.AbstractSymbolic, Vararg{Pair}}","page":"Reference/API","text":"replace(ex::SymbolicExpression, args::Pair...)\n\nReplace parts of the expression with something else.\n\nReturns a symbolic object.\n\nThe pattern/replacement is specified using variable => value; these are processed left to right.\n\nThere are different methods depending on the type of key in the the key => value pairs specified:\n\nA symbolic variable is replaced by the right-hand side, like ex(val,:), though the latter is more performant\nA symbolic parameter is replaced by the right-hand side, like ex(:,val)\nA function is replaced by the corresponding specified function, as the head of the sub-expression\nA sub-expression is replaced by the new expression.\nsub-expression containing a wildcard is replaced by the new expression. The specification of the replacements can use the wildcards. Wildcards expressions may be specified within SimpleExpressions or as Expr objects.\n\nExtended help\n\nThe first two styles are straightforward.\n\njulia> using SimpleExpressions\n\njulia> @symbolic x p\n(x, p)\n\njulia> ex = cos(x) - x*p\ncos(x) + (-1 * x * p)\n\njulia> replace(ex, x => 2) == ex(2, :)\ntrue\n\njulia> replace(ex, p => 2) == ex(:, 2)\ntrue\n\nThe third, is illustrated by:\n\njulia> replace(sin(x + sin(x + sin(x))), sin => cos)\ncos(x + cos(x + cos(x)))\n\nThe fourth is similar to the third, only an entire expression (not just its head) is replaced\n\njulia> ex = cos(x)^2 + cos(x) + 1\n(cos(x) ^ 2) + cos(x) + 1\n\njulia> @symbolic u\n(u,)\n\njulia> replace(ex, cos(x) => u)\n(u ^ 2) + u + 1\n\nReplacements occur only if an entire node in the expression tree is matched:\n\njulia> u = 1 + x\n1 + x\n\njulia> replace(u + exp(-u), u => x^2)\n1 + x + exp(-1 * (x ^ 2))\n\n(As this addition has three terms, 1+x is not a subtree in the expression tree.)\n\nThe fifth needs more explanation, as there can be wildcards in the expression. Wildcards can be specified as SimpleExpression expressions or as Expr objects.\n\nFirst, we describe the use of symbolic wildcards.\n\nWildcards have a naming convention using trailing underscores. One matches a single subexpression or term; two matches one or more subexpressions. In addition, the special symbol ⋯ (entered with \\cdots[tab] is wild.\n\njulia> @symbolic x p; @symbolic x_\n(x_,)\n\njulia> replace(cos(pi + x^2), cos(pi + x_) => -cos(x_))\n-1 * cos(x ^ 2)\n\n\njulia> ex = log(sin(x)) + tan(sin(x^2))\nlog(sin(x)) + tan(sin(x ^ 2))\n\njulia> replace(ex, sin(x_) => tan((x_) / 2))\nlog(tan(x / 2)) + tan(tan((x ^ 2) / 2))\n\njulia> replace(ex, sin(x_) => x_)\nlog(x) + tan(x ^ 2)\n\njulia> replace(x*p, (x_) * x => x_)\np\n\nPattern and replacements can also be specified with Julia expressions. The basic wildcard is prefaced with ~, a segment is specified with two ~.\n\njulia> ex = log(sin(x)) + tan(sin(x^2))\nlog(sin(x)) + tan(sin(x ^ 2))\n\njulia> replace(ex, :(sin(~x)) => :(tan(~x/2)))\nlog(tan(x / 2)) + tan(tan((x ^ 2) / 2))\n\njulia> replace(ex, :(sin((~x)^2)) => :(tan(~x)))\nlog(sin(x)) + tan(tan(x))\n\nUnlike symbolic wildcards, Expr objects can have default slot (specified as ~!x) and predicates (specified after :: to test.\n\njulia> pat, replacement = :(~!a * (~x)^(~n::(!=(-1)))), :(~a * (~x)^(~n+1)/(~n + 1))\n(:(~(!a) * (~x) ^ ~(n::(!=)(-1))), :((~a * (~x) ^ (~n + 1)) / (~n + 1)))\n\njulia> replace(x^2, pat => replacement)\n(x ^ 3) / 3\n\njulia> replace(x^(-1), pat => replacement)\n1 / x\n\n\nPicture\n\nThe AbstractTrees package can print this tree-representation of the expression ex = sin(x + x*log(x) + cos(x + p + x^2)):\n\njulia> print_tree(ex;maxdepth=10)\nsin\n└─ +\n   ├─ x\n   ├─ *\n   │  ├─ x\n   │  └─ log\n   │     └─ x\n   └─ cos              <--\n      └─ +             ...\n         ├─ x          <--\n         ├─ p          ...\n         └─ ^          ...\n            ├─ x       ...\n            └─ 2       ...\n\nThe command wildcard expression cos(x + ...) looks at the part of the tree that has cos as a node, and the lone child is an expression with node + and child x. The ⋯ then matches p + x^2.\n\n\n\n\n\n","title":"Base.replace"},{"category":"method","location":"reference/#CommonSolve.solve-Tuple{SimpleExpressions.SymbolicEquation, Union{SimpleExpressions.SymbolicParameter, SimpleExpressions.SymbolicVariable}}","page":"Reference/API","text":"solve(eq::SymboliclEquation, x)\n\nVery simple symbolic equations can be solved with the unexported solve method. This example shows a usage.\n\n@symbolic w p; @symbolic h  # two variables, one parameter\nimport SimpleExpressions: solve, D\nconstraint = p ~ 2w + 2h\nA = w * h\n\nu = solve(constraint, h)\nA = A(u) # use equation in replacement\nv = solve(D(A, w) ~ 0, w)\n\n\n\n\n\n","title":"CommonSolve.solve"},{"category":"function","location":"reference/#SimpleExpressions.combine","page":"Reference/API","text":"combine(ex, isconstant)\n\nLightly simplify symbolic expressions.\n\nExample\n\njulia> using SimpleExpressions: @symbolic, combine\n\njulia> @symbolic x p\n(x,)\n\njulia> ex = 1 + x + 2x + 3x\n1 + x + (2 * x) + (3 * x)\n\njulia> combine(ex)\n1 + (6 * x)\n\njulia> ex = 1 + x^2 + 2x^2 + 3x*x + x^4/x\n1 + (x ^ 2) + (2 * (x ^ 2)) + (3 * x * x) + ((x ^ 4) / x)\n\njulia> combine(ex)\n1 + (x ^ 3) + (6 * (x ^ 2))\n\njulia> combine(x + p*x)\n(1 + p) * x\n\n\nNot exported. This will cancel terms such as x/x.\n\n\n\n\n\n","title":"SimpleExpressions.combine"},{"category":"method","location":"reference/#SimpleExpressions.isconstant-Tuple{Number}","page":"Reference/API","text":"isconstant(x)::Bool\n\nCheck if x has a symbolic variable. (Symbolic parameters are treated as constant for this method.)\n\n\n\n\n\n","title":"SimpleExpressions.isconstant"},{"category":"method","location":"reference/#SimpleExpressions.isvariable-Tuple{Any}","page":"Reference/API","text":"isvariable(x)::Bool\n\nCheck if x is a symbolic variable or parameter\n\n\n\n\n\n","title":"SimpleExpressions.isvariable"},{"category":"method","location":"reference/#SimpleExpressions.map_matched-Tuple{Any, Any, Any}","page":"Reference/API","text":"map_matched(ex, is_match, f)\n\nTraverse expression. If is_match is true, apply f to that part of expression tree and reassemble.\n\nBasically CallableExpressions.expression_map_matched.\n\nNot exported.\n\n\n\n\n\n","title":"SimpleExpressions.map_matched"},{"category":"method","location":"reference/#SimpleExpressions.sympify-Tuple{String}","page":"Reference/API","text":"sympify(ex::String)\n\nTurn a string into an expression, converting symbols to symbolic variables.\n\nName comes from SymPy.\n\n\n\n\n\n","title":"SimpleExpressions.sympify"},{"category":"macro","location":"reference/#SimpleExpressions.@symbolic-Tuple","page":"Reference/API","text":"@symbolic x [p]\n\nCreate a symbolic variable and optional symbolic parameter.\n\nExpressions created using these variables subclass Function so may be used where functions are expected.\n\nThe  ~ infix operator can be used to create equations, which, by default, are treated as lhs - rhs when called as functions.\n\nExtended help\n\nCalling or substituting into expressions\n\nTo call a symbolic expression regular call notation with positional arguments are used. The first argument maps to any symbolic variable; the second – when given – to any symbolic parameter. It is an error to call an expression with a parameter using just a single argument; for that substitution is needed.\n\nExample\n\nusing SimpleExpressions\n@symbolic x p\nu = x^5 - x - 1\nu(2) # 29 call is u(x)\n\nu.((0,1,2)) # (-1, -1, 29)\n\nu = 2x + p\nu(1)    # errors!\nu(1, 2) # 2(1)+2 or 4\n\nu = sum(x .* p)\nu(2, [1,2]) # 6  call is u(x, p)\n\nCalling with nothing, missing, or : in a slot substitutes in the specified value leaving a symbolic expression, possibly with no variable or parameter.\n\n@symbolic x p\nu = cos(x) - p*x\nu(nothing, 2)  # cos(x) - 2 * x\nu(:, 2)        #  cos(x) - 2 * x, alternate calling form\nu(pi, nothing) # -1.0 - p * π\nv = u(1,:)(:,2)    # (cos(1)-(2*1)),\n\nThe latter can be evaluated using a zero-argument call, e.g. v().\n\nWith substitution in this manner, any symbolic variable and any symbolic parameters will receive the same substituted value.\n\nThe replace generic for symbolic objects takes pairs of values and replaces the left one with the right one working from left to right, leaving a symbolic expression. The replace method treats symbolic variables and symbolic parameters with different symbols as unique.\n\nA symbolic equation, defined through ~, may also be used to specify a left- and right-hand value.\n\nThe main use is as an easier-to-type replacement for anonymous functions, though with differences:\n\n1 |> sin(x) |> x^2  # 0.708… from sin(1)^2\nu = cos(x) - p*x\n2 |> u(:, 3) # -6.4161…, a alternative to u(2,3)\n\nmap(x^2, (1, 2)) # (1,4)\n\nSymbolic expressions an be used with other packages, to simplify some function calls at the expense of being non-idiomatic:\n\nusing Roots\n@symbolic x p\nfind_zero(x^5 - x - 1, 1)       # 1.167…\nfind_zero(x^5 - x ~ p, 1; p=4)  # 1.401…\n\nusing ForwardDiff\nBase.adjoint(𝑓::Function) = x -> ForwardDiff.derivative(𝑓, x)\nu = x^5 - x - 1\nfind_zero((u,u'), 1, Roots.Newton()) # 1.167…\n\nOr\n\nusing Plots\nplot(x^5 - x - 1, 0, 1.5)\n\nOr using both positions, so that we call as a bivariate function:\n\n@symbolic x y\nxs = ys = range(-5, 5, length=100)\ncontour(xs, ys, x^2 - y^2 + 2x*y)\n\nSymbolic derivatives can be taken with respect to the symbolic value, symbolic parameters are treated as constant. We use diff as an interface\n\n@symbolic x p\nu = x^5 - p*x - 1\ndiff(u, x)           # (5 * (x ^ 4)) - p\nu = u(:, 1)    # set parameter\na, b = 1, 2\nfind_zeros(diff(u,x) ~ (u(b)-u(a)) / (b-a), (a,b)) # [1.577…]\n\nIdiosyncrasies\n\nUsing this is a convenience for simple cases. It is easy to run into idiosyncrasies.\n\nExpressions are not functions in terms of scope\n\nUnlike functions, expressions are defined with variables at the time of definition, not when called. For example, with a clean environment:\n\n@symbolic x\nu = m*x + b    # errors, `m` not defined\nf(x) = m*x + b # ok\nm, b = 1, 2\nu = m*x + b    # defined using `m` amd `b` at time of assignment\nu(3)           # 1 * 3 + 2\nf(3)           # 1 * 3 + 2 values of `m` and `b` when called\nm, b = 3, 4\nu(3)           # still computing 1 * 3 + 2\nf(3)           # computing 3 * 3 + 4, using values of `m` and `b` when called\n\nSymbolic values are really singletons when calling by position\n\nThough one can make different symbolic variables, the basic call notation by position treats them as the same:\n\n@symbolic x\n@symbolic y    # both x, y are `SymbolicVariable` type\nu = x + 2y\nu(3)           # 9 coming from 3 + 2*(3)\n\nHowever, this is only to simplify the call interface. Using keyword arguments allows evaluation with different values:\n\nu(;x=3, y=2)   # 7\n\nUsing replace, we have:\n\nu(x=>3, y=>2)  # 3 + (2 * 2); evaluate with u(x=>3, y=>2)()\n\nThe underlying CallableExpressions object is directly called in the above manner; that package does not have the narrowed design of this package.\n\nContainers\n\nThe variables may be used as placeholders for containers, e.g.\n\nu = sum(xi*pi for (xi, pi) in zip(x,p))\nu((1,2),(3,4))  # 11\n\nBroadcasting as a function\n\nBroadcasting a function call works as expected\n\n@symbolic x\nu = x^2\nu.((1,2)) # (1, 4)\n\nSymbolic expressions can also be constructed that will broadcast the call\n\nu = x.^2 .+ sin.(p)\nu((1,2),3)\n\nu = @. x^2 + sin(p)\nu((1,2),(3,4))\n\n\n\n\n\n","title":"SimpleExpressions.@symbolic"},{"category":"macro","location":"reference/#SimpleExpressions.@symbolic_variables-Tuple","page":"Reference/API","text":"@symbolic_variables w x[1:3] y() z=>\"𝑧\" Ω::isinteger\n\nDefine multiple symbolic variables or symbolic functions. Guards are currently ignored.\n\nNot exported.\n\njulia> using SimpleExpressions: @symbolic_variables\n\njulia> @symbolic_variables  w x[1:3] y() z=>\"𝑧\" Ω::isinteger\n(w, SimpleExpressions.SymbolicVariable[x₁, x₂, x₃], y, 𝑧, Ω)\n\njulia> x\n3-element Vector{SimpleExpressions.SymbolicVariable}:\n x₁\n x₂\n x₃\n\n\n\n\n\n","title":"SimpleExpressions.@symbolic_variables"},{"category":"section","location":"#SimpleExpressions.jl","page":"Home","text":"Documentation for SimpleExpressions, a very lightweight means to create callable functions using expressions.\n\nThis package leverages the CallableExpressions package for the heavy lifting.","title":"SimpleExpressions.jl"},{"category":"section","location":"#Rationale","page":"Home","text":"Julia has easy-to-use \"anonymous\" functions defined through the pattern (args) -> body using ->, notation which mirrors common math notation. However, for students the distinction between an expression, such as defines the \"body\" and a function is sometimes not made, whereas in Julia or other computer languages, the distinction is forced. The SymPy package—as well as other symbolic packages in Julia like Giac, Symbolics and SymEngine—allows callable symbolic expressions to be created naturally from symbolic variables by adding methods to many of Julia's mathematical function. This package does just this (but does not provide the many other compelling features of a CAS). The symbolic expressions subtype Function, so can be used where functions are expected.\n\nTo keep things as simple as possible, there are only a few types of symbolic values: symbolic numbers, symbolic variables, symbolic parameters, symbolic expressions, and symbolic equations.\n\nSymbolic variables and parameters are created with the @symbolic macro. For the @symbolic macro, the first argument names the symbolic variable, the optional second argument names the symbolic parameter. (The unexported @symbolic_variables macro can define multiple variables with one call.)\n\nSymbolic expressions are built up naturally by using these two types of objects.\n\nSymbolic equations are specified with the infix ~ operator with the left- and right-hand sides being symbolic expressions.\n\nSymbolic numbers can be produced from substitution.\n\nThe symbolic expressions are just \"thunks\" or delayed expressions (akin to Thunks.jl) but implemented in a more performant manner in CallableExpressions, where the operation and its arguments are kept in a structure and the expression is evaluated when called as a function.","title":"Rationale"},{"category":"section","location":"#Usage","page":"Home","text":"A quick example showing how expressions may be defined and called:\n\nusing SimpleExpressions\n\n@symbolic x p\nu = exp(-x) * (sin(3x) + sin(101*x))\nu(2)\n0.07132208791095426\n\nThis is akin, but different from using a function:\n\nf(x) = exp(-x) * (sin(3x) + sin(101*x))\nf(2)\n0.07132208791095426\n\nThe main difference being, u can subsequently be algebraically manipulated.\n\nThe parameter can also be used to form an expression:\n\nu = cos(x) - p * x\nu(pi/4, 4)\n-2.4344858724032457\n\nThe variable or parameter can be substituted in for using positional arguments:\n\nu(pi/4,:), u(:, 4)\n(cos(0.7853981633974483) + (-1 * p * 0.7853981633974483), cos(x) + (-1 * 4 * x))\n\nOr, the expression can be evaluated directly\n\nu(pi/4, 4)\n-2.4344858724032457","title":"Usage"},{"category":"section","location":"#Evaluation","page":"Home","text":"The basic calling pattern for a symbolic expression ex is simple: the first positional argument is for the symbolic value, the second for the symbolic parameter. All symbolic variables in ex are evaluated at the same value; all symbolic parameters in ex are evaluated at the same value.\n\nThis simplification leads to these rules:\n\nex(x) is used to evaluate the expression of just the variable with the value of  x; an error is thrown if the expression has both a variable and a parameter.\nex(x, p) is used to evaluate an expression of both a  variable and a parameter; if there is no parameter the value of the second argument is ignored.\nex(*, p) is used to evaluate an expression of just a parameter. The * in the x slot can be any valid identifier (except for :, nothing, or missing, as they are used for substitution); the value of the first argument is just ignored.\nex() is used to evaluate an expression that involves neither a symbolic variable or a parameter, such as can be produced through substitution.","title":"Evaluation"},{"category":"section","location":"#Substitution","page":"Home","text":"Evaluation leaves a non-symbolic value. For substitution, the result is still symbolic.\n\nThe basic syntax for substitution is:\n\nex(:, p) to substitute in for the parameter.\nex(x, :) to substitute in for the variable.\n\nThe use of : to indicate the remaining value is borrowed from Julia's array syntax; it can also be either nothing or missing.\n\nFor substitution using positional arguments (as with evaluation), all instances of symbolic variables and all instances of symbolic parameters are treated identically.\n\nWhen a key=>value pair is used as the argument, if the key is a symbolic variable or a symbolic parameter, then that object is replaced by the value on the right-hand side.\n\n@symbolic x p\nu = (p + sqrt(x))/2\nu(:,1), u(5, :) # symbolic, not numeric\n((1 + sqrt(x)) / 2, (p + sqrt(5)) / 2)\n\nu(5, 1), u(x=>5, p=>1) # golden ratio numerically, symbolically\n(1.618033988749895, (1 + sqrt(5)) / 2)","title":"Substitution"},{"category":"section","location":"#Symbolic-containers","page":"Home","text":"The values for x or p may be replaced by containers. For example:\n\n@symbolic x a\nu = sum(aᵢ * x^(i-1) for (i,aᵢ) ∈ enumerate(a))\nu(2, (1,2,3,4)) # 49\n49\n\nThis is relatively untested and almost certainly not fully featured. For example, only evaluation is allowed, not substitution (using :):\n\n@symbolic x a\nu = sum(ai * x^(i-1) for (i,ai) in enumerate(a))\nu(2, [1,2,3])\n17","title":"Symbolic containers"},{"category":"section","location":"#Broadcasting","page":"Home","text":"The package is intended to support broadcasting of expressions and the construction of broadcasting expressions.\n\n@symbolic x p\nu = x^2 + p\nv = @. x^2 + p\nf(x,p) = x^2 + p\nx0, p0 = (1,2), (3,4)\nu.(x0, p0) == v(x0, p0) == f.(x0, p0) == (1^2+3, 2^2+4)\ntrue","title":"Broadcasting"},{"category":"section","location":"#Equations","page":"Home","text":"The package grew out of a desire to have a simpler approach to solving f(x) = g(x). While defining h(x) = f(x) - g(x) and solving h(x) = 0 using, say, Roots is straightforward, it does cause confusion while learning.\n\nSymbolic equations are specified using ~, a notation borrowed from Symbolics for SymPy and now on loan to SimpleExpressions. Of course = is assignment, and == and === are used for comparisons, so some other syntax is necessary and ~ plays the role of distinguishing the left- and right-hand sides of an equation.\n\nBy default, when calling a symbolic equation the difference of the left- and right-hand sides is used, so, in this case, symbolic equations can be passed directly to the find_zero method from Roots:\n\nusing Roots\n@symbolic x p\nfind_zero(cos(x) ~ sin(x), (0, pi/2)) # use bisection\n0.7853981633974484\n\nThe solve interface (loaded with Roots) is also available for symbolic equations:\n\nsolve(cos(x) ~ p*x, (0, pi/2), p=3)\n0.3167508287712212\n\nLinear symbolic equations can be solved symbolically through this package (though the lack of simplification is annoying). Instead of specifying an interval, a variable to solve for is given.\n\n@symbolic a A\n@symbolic b B\nsolve(sin(A)/a ~ sin(B)/b, A)  # solve not exported, but is imported with Roots above\nA ~ asin(a * (sin(B) / b))\n\nThis example shows \"inverse\" functions are applied (without concern for domain/range restrictions) when possible.","title":"Equations"},{"category":"section","location":"#Plotting","page":"Home","text":"For plotting a symbolic equation, eq, the values eq.lhs and eq.rhs may be used separately to produce a pair of traces. With Plots, where a vector of functions may be plotted, plot([eq...], a, b) will plot each side with separate trace. Though with Plots there is a recipe to plot a symbolic equation as two separate functions.","title":"Plotting"},{"category":"section","location":"#Derivatives","page":"Home","text":"Symbolic expressions can be easily differentiated. A method for diff is used, as that name is established in some other languages. A variable to differentiate by should be specified. The operator differentiates with respect to the variable assuming it represents a scalar quantity:\n\n@symbolic x p\ndiff(cos(x) - x * p, x)\n(-1 * p) + (-1 * sin(x))\n\ndiff(cos(x) ~ x * p, p)\n0 ~ x\n\nHere the derivative is used to take a step of Newton's method::\n\nu = x^5 - x - 1\ndu = diff(u, x)\nx0 = 2\nx0 - u(x0) / du(x0)\n1.6329113924050633\n\nHere the application of the product rule can be seen:\n\nu = diff(exp(x) * (sin(3x) + sin(101x)), x)\n(((3 * cos(3 * x)) + (101 * cos(101 * x))) * exp(x)) + ((sin(101 * x) + sin(3 * x)) * exp(x))","title":"Derivatives"},{"category":"section","location":"#replace","page":"Home","text":"To work with multiple symbolic parameters or variables, replace can be used to substitute in values for a specific variable.\n\nreplace(ex, args::Pair...) to substitute in for either a variable, parameter, expression head, or symbolic expression (possibly with a wildcard). The pairs are specified as variable_name => replacement_value.\nWhen a symbolic expression is called with a key=>value pair, the replace method is used. That is ex(args::Pair...) redirects to replace(ex, args::Pair...).\n\nTo illustrate, two or more variables can be used, as here:\n\n@symbolic x\n@symbolic y  # or SimpleExpressions.@symbolic_variables x y\nu = x^2 - y^2\n(x ^ 2) + (-1 * (y ^ 2))\n\nEvaluating u with a value in the x position will evaluate both x and y with that value:\n\nu(1)    # always 0\nu(1, 2) # not 1^2 - 2^2, the second argument is ignored here\n0\n\nAs indicated, this is a deliberate design limitation to simplify usage. It can be worked around via replace:\n\nv = u(x=>1, y=>2)          # the symbolic value ((1^2)-(2^2)), also replace(u, x=>1, y=>2)\nv()                        # evaluates to -3\n-3\n\nThe replace method is a bit more involved than illustrated. The key => value pairs have different dispatches depending on the value of the key. Above, the key is a SymbolicVariable, but the key can be:\n\nA SymbolicVariable or SymbolicParameter in which case the simple substitution is applied, as just illustrated.\nA function, like sin. In this case, a matching operation head is replaced by the replacement head. Eg. sin => cos will replace a sin call with a cos call.\n\nv = sin(x) + sin(x^2)\nreplace(v, sin => cos)\ncos(x) + cos(x ^ 2)\n\nA symbolic expression. In this case, the exact match of the expression is replaced by the replacement value.\n\nv = 1 + (x+1)^1 + 2*(x+1)^2 + 3*(x+1)^3\nreplace(v, x+1 => x)\n1 + x + 1 + (2 * (x ^ 2)) + (3 * (x ^ 3))\n\nA symbolic expression with one or more wildcards. Wildcards have a naming convention using trailing underscores. One matches one value; two matches one or more values; three match 0, 1, or more values. In addition, the special symbol ⋯ (entered with \\cdots[tab] is wild. (The same pattern can't use, say, x_ and x__.)\n\nv = log(1 + x) + log(1 + x^2/2)\n@symbolic x_\nreplace(v, log(1 + x_) => log1p(x_)) # log1p(x) + log1p((x ^ 2) / 2)\nlog1p(x) + log1p((x ^ 2) / 2)\n\nSubstitution uses match(pattern, subject) for expression matching with wildcards:\n\nsubject, pattern = log(1 + x^2/2), log(1+x_)\nσ = match(pattern, subject)\nBase.ImmutableDict{Symbol, SimpleExpressions.AbstractSymbolic} with 1 entry:\n  :x => (x ^ 2) / 2\n\nThe return value is a dictionary of valid substitutions with a special sentinel for no match. The non-exported rewrite method can be used to substitute this dictionary into a replacement pattern.\n\nUsing the original pattern should result in an expression equivalent to the original subject.\n\nSimpleExpressions.rewrite(σ, pattern)\nlog(1 + ((x ^ 2) / 2))\n\nThis is the final step in the last replace call above.\n\nSimpleExpressions.rewrite(σ, log1p(x_))\nlog1p((x ^ 2) / 2)","title":"replace"},{"category":"section","location":"#Simplification","page":"Home","text":"No simplification is done by default so the expressions can quickly become unwieldy. The unexported combine functions does light simplification by combining like terms for addition and multiplication.\n\nex = 1 + x + 2x + 3x + 4\n1 + x + (2 * x) + (3 * x) + 4\n\nusing SimpleExpressions: combine\ncombine(ex)\n5 + (6 * x)\n\nUnlike most computer algebra systems—where the basic representation includes specialized storage for additive and multiplicative terms—this package does not. The combine function represents terms in this other manner, combines like terms, and then creates a expression again. This is a bit excessive to make a default, as the point here is reasonably fast callable functions.\n\nThere is also an unexported simplify command that uses pattern matching to perform basic simplification.\n\nusing SimpleExpressions: simplify\nex = 10 * sin(2x)^2 + 10 * cos(2x)^2 - 5\nsimplify(ex)\n5","title":"Simplification"}]
}
